"""Step 3: which entity is this span? (page to come)"""

import streamlit as st

from ngec_demo.style import lede

st.title("3. Which entity?")
lede("Each actor span is matched against Wikipedia and the candidates are ranked.")
st.info("This page is not built yet.")
