"""Step 1: which event type? (page to come)"""

import streamlit as st

from ngec_demo.style import lede

st.title("1. Which event?")
lede("Sixteen event types, one classifier each, with its own threshold.")
st.info("This page is not built yet.")
