"""Support code for the NGEC Streamlit demo.

`resources` loads and caches the models, `steps` wraps one pipeline stage each
and returns JSON-safe dicts, `examples` holds the inputs the pages open with,
and `style` holds the CSS and the two shared charts.
"""
