## Setup

This directory includes code for training custom event detection models and the
actor/wiki model. Please see each directory for code and documentation.

Code for building the offline Elasticsearch indices (`wiki` and `geonames`) now
lives in [`elasticsearch/`](../elasticsearch/README.md).